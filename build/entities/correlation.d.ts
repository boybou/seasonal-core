export declare class Correlation {
    constructor(firstMarketId: number, secondMarketId: number, period: number, value: number);
    firstMarketId: number;
    secondMarketId: number;
    period: number;
    value: number;
}
